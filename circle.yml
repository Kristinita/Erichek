machine:
  python:
    version: 3.6.3

dependencies:

  # [DONE]
  # # [FIXME]: temporary solution, until pyfancy bug not fix
  # # https://stackoverflow.com/a/37375913/5951529
  # # https://github.com/ilovecode1/Pyfancy-2/issues/31#issuecomment-362977812
  # override:
  #   - echo "Not install dependencies from requirements.txt"

  post:
    - pip install flake8
    - pip install pydocstyle
    - pip install pyfancy
    - pip install pyroma
    - pip install -U setuptools
    - pip install vulture

test:
  override:
    - flake8 .
    - pydocstyle
    - pyroma .
    - vulture .